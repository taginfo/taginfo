<%
@title.unshift(h(t.osm.keys))
%>

<h1><a href="<%= build_link("/sources/#{ @source.id }") %>"><%= @source.name %></a>:
    <%= h(t.osm.keys) %></h1>

<p><%= h(t.pages.sources.wikidata.keys.description) %></p>

<div class="box">
    <div id="grid-items"></div>
</div>
